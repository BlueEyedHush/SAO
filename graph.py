class Graph(object):
    def __init__(self):
        self.vertices_num = 0
        self.nodes = set()
        self.starting_vertices = list()
        super(Graph, self).__init__()

    def from_file(self, input_file):
        """ Generate graph from file format:
        n m s
        1 4
        1 2
        2 3
        ...
        where n is the number of vertices, m number of edges and s number of starting vertices
        next line lists starting vertices
        and the following lines determine edges
        this is exactly the format generated by the generate utility
        """
        with open(input_file, 'r') as f:
            self.vertices_num, _ = map(int, f.readline().split())
            self.starting_vertices = f.readline().split()
            self.starting_vertices = [Node(s) for s in self.starting_vertices]
            for node_id in xrange(self.vertices_num):
                self.nodes.add(Node(node_id))
            for line in f:
                v1, v2 = line.split()
                v1, v2 = Node(v1), Node(v2)
                v1.add_neighbor(v2)
        return self.starting_vertices


class Node(object):
    def __init__(self, node_id, value=None):
        self.id = node_id
        self.neighbors = set()
        self.value = value
        super(Node, self).__init__()

    def add_neighbor(self, node):
        self.neighbors.add(node)
        node.neighbors.add(self)

    def print_graph(self):
        """ Print the graph structure accessible from this node """
        raise NotImplementedError

    def __eq__(self, other):
        return self.id == other.id
